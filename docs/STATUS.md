# Status Atual

> Atualizado em 2026-08-27 (Tasks R.5a e R.5b mescladas — composição e acabamento terminal das telas Orçamento e Editor de Item). Este arquivo é o ponto de partida de qualquer
> sessão nova. Histórico task-a-task (auditorias, tentativas, decisões)
> não vive mais aqui — está no `git log` e nos commits de merge de cada
> branch `feature/<task-id>`. Este arquivo mantém só o essencial.

## 1. Onde estamos

**Épico V2 completo** (Fases A–C); **Épico V2.1/Fase D em execução** (Lote 0
iniciado 2026-08-03). Painel de orçamento para marceneiros: motor de caixa
paramétrico (V3), persistência multi-tenant real via Supabase, todas as telas
da experiência construídas. Fases A (Discovery), B (Motor e dados) e C
(Experiência/telas) — todas concluídas. 

Fase D (Pré-Lançamento) — Lote 0 (Fundação de dados) ✅ fechado:
**Tasks 0.1–0.3, 0.4, 0.5a, 0.7a, 0.7b, 0.5b mescladas** (wiring multi-Ambiente/Parede, migration, Server Actions, alturas de faixa, dados de cliente, congelamento real, formulário de cliente — Lote 0 completo 6/6).

**Lote 1 (Confiança e estado) ✅ fechado por completo** — 7/7 tasks concluídas (5/5 back + 2/2 front).

**Lote 2 (Lacunas funcionais) — 18/18 tasks concluídas** ✅

**Lote 3 (Precisão do motor) ✅ fechado por completo — 14/14 tasks concluídas** — Tasks 3.1/3.3 (motor + front), 3.2, 3.4, 3.5 (motor + front), 3.6, 3.7, 3.8 (back + front), 3.10–3.11 (motor + front), 3.12, 3.13 (catálogo-back), 3.13 (catálogo-front) e 3.13 (front) mescladas:
- **Task 3.1/3.3 (motor)** ✅ (2026-08-11) — Bin-packing melhorado: guilhotina com lista de retângulos livres + simulated annealing (PRNG semeado, `MAX_ITERACOES=5000` fixo). Reescrito em `lib/engine/box/cutting.ts`. Kerf como parâmetro opcional em `GrupoChapas.meta?`, integração em Web Worker será na Task 3.1/3.3 (front). Todas as 7 invariantes V1–V7 (Modelo 8.5) com teste dedicado; 3 exemplos numéricos reproduzidos com valores exatos. `code-auditor` APROVADO (1ª real), `qa-engineer` APROVADO (1ª, 466 testes, verificação aritmética manual). **Desbloqueia Task 3.1/3.3 (front).**
- **Task 3.1/3.3 (front)** ✅ (2026-08-11) — Web Worker para `planoDeCorte` com estado UI `calculando → pronto`, estimativa síncrona + busca assíncrona, fallback para main thread. Kerf real do perfil (`espessuraSerraPadraoMm`, Task 4.16-back) integrado em `CorteMaterialLab` e `EditorItemNucleo`. Indicador "Otimizando plano de corte…" visual. `code-auditor` APROVADO (3ª convocação real), `qa-engineer` APROVADO (2ª tentativa, 472 testes ao final, teste de fallback coberto). **`ux-auditor` APROVADO (1ª tentativa real, screenshots confirmando estimativa síncrona sempre visível, sem flash de layout na substituição pelo resultado do Worker, tokens do Design System conformes)** — 2 observações não bloqueantes: (1) Critério do contrato sobre "canvas vazio em loading" não se aplica a este fluxo (a estimativa síncrona já preenche desde primeiro render); (2) `EditorItemNucleo.tsx` tem mistura pré-existente de classes CSS legadas com tokens Tailwind, fora do escopo desta task. Impacto Visual: Leve. **Fecha a Task 3.1/3.3 (motor + front) por completo — itens 3.1 (bug do marceneiro, faixa livre ignorada) e 3.3 (avaliação de troca de algoritmo) do backlog-fonte resolvidos ponta a ponta.**
- **Task 3.5 (motor)** ✅ (2026-08-11) — Fita de borda discriminada por cor: agregação em `lib/engine/consolidar.ts` com `larguraFitaMm()` (regra "menor fita ≥ espessura final" via `[22,35,65]`), `somaFita()` (Map por `cor|largura`), tipo `GrupoFita`, campo `fitaPorCor` em `EngineOutput["consolidado"]`. `fitaTotalM` preservado — invariante soma(fitaPorCor)===fitaTotalM verificada manualmente. 478 testes (10 novos), zero regressão. `code-auditor` APROVADO (1ª), `qa-engineer` APROVADO (1ª). **Desbloqueia Task 3.5 (front) e Task 3.6.**
- **Task 3.10–3.11 (motor)** ✅ (2026-08-11) — Tampo: tipo `ModeloTampo` e `ConfigTampo` em `lib/engine/elemento-continuo/types.ts`; `modeloDoTampo()` deriva `modelo` de `engrossamento` via projeção pura (impossibilita invariante `modelo ⟺ engrossamento` ser violada); `ESPESSURAS_VALIDAS_POR_MODELO_TAMPO` (simples 15/18/25; engrossado/dobrado base 15 → 30/45/60; base 18 → 36/54); funções de validação/operação em `lib/engine/elemento-continuo/explode.ts` (`validarEspessuraTampo`, `espessuraFinalDoConfigTampo`, `validarConfigTampo`, `trocarModeloTampo` — limpa espessura ao trocar modelo incompatível). `explodeTampo` calcula `fitaM` real. 490 testes (15 novos), zero regressão. 3 exemplos numéricos e 3 exemplos de rejeição reproduzidos. `code-auditor` APROVADO (1ª), `qa-engineer` APROVADO (1ª). **Desbloqueia Task 3.10–3.11 (front) e Task 3.12.**
- **Task 3.10–3.11 (front)** ✅ (2026-08-11) — UI do tampo em `AmbientesLab.tsx`: seletor de modelo (Simples/Engrossado/Dobrado) antes do seletor de Espessura, só quando tipo de elemento contínuo é "tampo". Lista de espessuras filtrada por modelo via `ESPESSURAS_VALIDAS_POR_MODELO_TAMPO`. Troca de modelo limpa espessura incompatível no estado. Construção de `engrossamento` extraída como função pura `montarEngrossamentoTampo(selecao)` com defaults documentados (4 lados, sarrafo 70mm para engrossado). **2 rodadas de gate**: `qa-engineer` reprovou 1ª tentativa por falta de teste automatizado, corrigido com 4 casos (simples/engrossado 30mm/engrossado 54mm/dobrado 60mm), **APROVADO 2ª tentativa**. `code-auditor` APROVADO (1ª), `ux-auditor` APROVADO (1ª, 6 screenshots reais confirmando modelo antes de espessura, lista mudando por modelo, campo limpo ao trocar modelo incompatível, ausência de seletor em rodapé). Sem `security-auditor` (sem RLS/segredo). 563 testes, zero regressão. Impacto Visual: Leve. **Fecha a Task 3.10–3.11 (motor + front) por completo — RF-28 resolvido ponta a ponta.**
- **Task 3.8 (back)** ✅ (2026-08-11) — Persistência de override de quantidade por item da lista de material: migration `supabase/migrations/20260811110000_lista_material_override.sql` cria tabela `lista_material_override` (colunas `organizacao_id`, `orcamento_id`, `item_chave`, `quantidade`; UNIQUE `(orcamento_id, item_chave)`, CHECK `quantidade >= 0`; RLS com SELECT/INSERT/UPDATE/DELETE escopadas por `organizacao_id`). Server Actions `definirOverrideQuantidade`, `removerOverrideQuantidade` e leitura `listarOverridesQuantidade` em `lib/lista-material/override.ts` com checklist de posse (organização do servidor, rejeição cross-tenant). Rejeição por orçamento congelado em ambas Server Actions de escrita, sem bypass. 506 testes (10 novos), zero regressão. `code-auditor` APROVADO (1ª), `security-auditor` APROVADO (1ª, RLS + posse + congelamento verificados), `qa-engineer` APROVADO (1ª, 5 critérios + posse cruzada). Sem UI/ux-auditor. **Migration NÃO aplicada no Supabase real** — bloqueio de CLI pendente operador. **Desbloqueia Task 3.8 (front).**
- **Task 3.13 (catálogo-back)** ✅ (2026-08-11) — Bucket de Storage `texturas` (read-only `authenticated`, escrita `service_role`), campo `texturaUrl?: string` em `EspecificacaoChapa`, validação anti-hotlink em `lib/produto/acoes.ts`. **2 rodadas de gate**: `security-auditor` reprovou 1ª tentativa por bypass no `.trim()` da validação — correção: `.replace(/[\u0000-\u0020\u007f]/g, "")` com testes de 10 variantes de bypass. `code-auditor` APROVADO (1ª), `security-auditor` APROVADO (2ª), `qa-engineer` APROVADO (1ª, 504 testes, 8 novos). Sem UI. **Migration NÃO aplicada no Supabase real** — bloqueio de CLI pendente operador. **Desbloqueia Task 3.13 (catálogo-front).**

**Lote 5 — 5/5 tasks concluídas ✅ (2026-08-17) (5.1–5.4 + 5.10-back + 5.5-5.6 + 5.7-5.9 + 5.10-front mescladas)**:
- **Task 5.1–5.4** ✅ (2026-08-17) — Migração de `/biblioteca` e `/modulo` para shell/Design System v3 (RF-35): **investigação prévia confirmou que `/biblioteca` já estava 100% migrada desde Task 13.7c** (sem rótulo legado, sem CSS antigo), sem mudança lá. Escopo real: `/modulo` — rota `app/modulo/page.tsx` → `app/(app)/modulo/page.tsx` (mesma URL pública, agora dentro do shell com sidebar/topbar), wrapper legacy removido (shell provê container via `main p-xl`), 3 links de texto herdados removidos, navegação apenas pela sidebar. Entrada em `TITULOS_POR_ROTA` (`components/shell/Topbar.tsx`) para "Editor de módulo". Harness novo `app/dev/preview/modulo/page.tsx` (404 em produção), registrado em `lib/auth/rotas.ts`. Comentários stale em `middleware.ts`/`lib/auth/rotas.ts` atualizados. **Gap deliberado registrado**: `EditorItemNucleo.tsx` e componentes-filho continuam com CSS legado (`.card`/`.legado-grid`, `app/globals.css`) por compartilharem núcleo com `/orcamento/[id]/item/[itemId]` em produção — retrofit risco maior, candidato a task futura de UI (gap registrado em `docs/Backlog.md` §"Gaps de lógica/design"). `code-auditor` APROVADO (1ª), `qa-engineer` APROVADO (1ª, 641 testes), `ux-auditor` APROVADO (1ª, 6 screenshots). Sem `security-auditor` (rota/layout). Impacto Visual: Completo.
- **Task 5.5–5.6** ✅ (2026-08-14) — Remover vocabulário de especificação vazado na UI (RF-36): título do card de validação, rótulos "Bancada (mm)" → "Meio (mm)", "Pé-direito (mm)" → "Limite superior do aéreo (mm)". Identificadores de código preservados. `code-auditor` APROVADO (1ª tentativa real), `qa-engineer` APROVADO (1ª), `ux-auditor` APROVADO (1ª). Sem `security-auditor`. Impacto Visual: Leve. **Observação não bloqueante**: `ElementoParedePreset` card usa ainda "preset" (diferente conceito, fora do escopo) — candidato a task futura de consistência.
- **Task 5.7–5.9** ✅ (2026-08-15) — Dashboard troca prazo de entrega por valor final e custo (RF-33): coluna "Prazo de entrega" removida da tabela "Orçamentos recentes" (`components/dashboard/DashboardView.tsx`); colunas "Valor final" (`resumo.precoFinal`) e "Custo" (`resumo.custoMaterial`, não custo total) adicionadas, formatadas com `formatarMoeda` + `tabular-nums`. `lib/dashboard/orcamentos.ts` ganhou `calcularResumoDoOrcamento` server-side, mirror do pipeline já usado pela aba Financeiro, com `try/catch` isolado por orçamento e catálogo buscado uma única vez por carregamento. Novo arquivo `lib/produto/buscarServer.ts`. Tipo `OrcamentoDashboardRow` perde `prazoEntrega`, ganha `valorFinal`/`custoMaterial` (`number | null`). 4 testes novos em `lib/dashboard/orcamentos.test.ts`, 637/637 passando. `code-auditor` APROVADO (1ª), `qa-engineer` APROVADO (1ª), `ux-auditor` APROVADO (1ª). Sem `security-auditor`. Impacto Visual: Leve. **Simplificação deliberada (ponytail)**: valor sempre recalculado ao vivo, mesmo congelado — não persiste `linhaProposta.valorRateado`. Aceito porque Dashboard é visão geral, não documento legal; fonte de verdade congelada continua sendo Proposta/PDF.
- **Task 5.10 (front)** ✅ (2026-08-16) — Seletor de etapa de esteira + badge `rotuloDoCard` (RF-33, Modelo-de-Domínio Seção 7.2, Q-6/Q-15 resolvidas): função `rotuloDoCard(status, etapaEsteira)` em `components/dashboard/StatusOrcamentoBadge.tsx` (3 ramos: `fechado` → "Fechado"; `visita_agendada`/`projeto_3d`/`aguardando_aprovacao` → "Em andamento"; `novo` → rótulo do status comercial reaproveitando `ROTULO`/`VARIANTE`). Badge em Dashboard (já existente, agora com etapa) e `/orcamento/[id]` (novo, cabeçalho do orçamento). Novo Client Component `components/orcamento/SeletorEtapaEsteira.tsx`: `<Select>` com 4 etapas não-terminais, read-only quando `etapa=fechado`, erro inline em falha. `etapaEsteira` exposta em `buscarOrcamentoPorId`, `buscarItemDoOrcamento`, `buscarDadosDashboard`. 641/641 testes passando. **Correção de arquitetura**: Server Action `atualizarEtapaEsteira` tinha `"use server"` inline em arquivo com funções síncronas, quebrando import por Client Component — extraída para arquivo novo `lib/orcamento/atualizarEtapaEsteira.ts` com `"use server"` no topo. `code-auditor` APROVADO (1ª), `qa-engineer` APROVADO (1ª), `ux-auditor` APROVADO (1ª, 8 screenshots). Sem `security-auditor` (Server Action já auditada em Task 5.10-back). Sem migration. Impacto Visual: Leve.

**Pipeline Stage — Reparação iniciado (2026-08-25)**:
- **Task R.1** ✅ (2026-08-25) — Direção de Arte (Seção 0 do Design System): Seção 0 escrita no topo de `docs/Design-System.md`; teste de identidade aplicado; tese visual "prancheta de bancada com tela de gestão em volta"; decisão de assinatura: traço de cota + generalização de régua/seta já existentes (§9.3/9.4); paleta v3 (navy + laranja) preservada intacta com nova justificativa. Produto = orçamento e projeto para marceneiro; vocabulário técnico 2D (elevação, plano de corte, cota, hachura, medida) ancorado em domínio. Revisão final: teste de identidade APROVADO, paleta preservada. `product-designer`. Impacto Visual: Leve.
- **Task R.2a** ✅ (2026-08-25) — Composição de Tela (5 telas críticas): `docs/Screen-Composition.md` criado com composição de Orçamento (`/orcamento/[id]`), Editor de Item (`/orcamento/[id]/item/[itemId]`), Catálogo (`/catalogo`), Biblioteca (`/biblioteca`), Proposta impressa (`/proposta/[id]/pdf`, nível `vitrine`). Ancorado no código real, densidade explícita (Catálogo/Biblioteca densas, Editor com coluna decisão espaçosa/coluna técnica densa), seção "Poda" em cada tela com destino explícito. Revisor: todos critérios do contrato atendidos, nenhum token inventado. Achado não bloqueante registrado: plano de corte do Editor deveria estar na coluna direita (hoje à esquerda) — divergência do Mapa-de-Telas §3.7, corrigida na composição como Poda. `product-designer`. Impacto Visual: Leve.
- **Task R.2b** ✅ (2026-08-25) — Composição de Tela (7 telas restantes): `docs/Screen-Composition.md` expandido com Dashboard (`/`), Novo orçamento (`/orcamento/novo`), Perfil (`/perfil`), Materiais (`/configuracoes/materiais`), Proposta (`/proposta`), Login (`/login`) e Signup (`/signup`), totalizando 12 telas. Ancorado no código real, seção "Poda" em cada tela com destino explícito, Login e Signup marcadas nível `vitrine`. Todos os critérios do contrato atendidos. Achados não bloqueantes registrados: `/configuracoes/materiais` (V2 órfão), `/proposta` (código morto), Dashboard/Novo orçamento (duplicação de título), Login/Signup (ícone inconsistente) — 4 observações formais para decisão explícita sobre virarem task. `product-designer`. Impacto Visual: Leve.
- **Task R.3a** ✅ (2026-08-25) — Decompor `components/ambientes/AmbientesLab.tsx`: arquivo reduzido de 2.250 para 173 linhas (wrapper de composição), 24 arquivos extraídos (maior: 243 linhas), estado agrupado em 7 hooks de domínio + seções de apresentação (`Secao*.tsx`), funções puras/constantes/tipos em módulos irmãos. Decomposição pura sem mudança de comportamento/aparência. `code-auditor` APROVADO (1ª tentativa), `qa-engineer` APROVADO (1ª tentativa, 641/641 testes, mesma contagem do baseline). Sem `security-auditor`/`ux-auditor` (refatoração pura, contrato dispensa). Nenhum Circuit Breaker. `frontend-engineer`. Impacto Visual: Nenhum.
- **Task R.3b** ✅ (2026-08-25) — Decompor `app/components/BoxCanvas.tsx`: arquivo reduzido de 1.086 para 269 linhas (wrapper de composição), 5 arquivos extraídos (`BoxCanvas.types.ts` 107 linhas, `lib/engine/box/canvas-geometria.ts` 225, `canvas-desenho-partes.ts` 209, `canvas-desenho-item-unico.ts` 152, `canvas-desenho-conjunto.ts` 238). Decomposição pura sem mudança de comportamento/aparência. Consumidores preservados: `EditorItemNucleo.tsx`, `LinhaPropostaCard.tsx`, `SecaoElevacaoConjunto.tsx` (import path e assinatura intactos). `code-auditor` APROVADO (1ª tentativa), `qa-engineer` APROVADO (1ª tentativa, 641/641 testes, mesma contagem do baseline). Sem `security-auditor`/`ux-auditor` (refatoração pura, contrato dispensa). Nenhum Circuit Breaker. `frontend-engineer`. Impacto Visual: Nenhum.
- **Task R.3c** ✅ (2026-08-26) — Decompor `app/modulo/EditorItemNucleo.tsx`: arquivo reduzido de 1.003 para 11 arquivos (maior: `EditorItemNucleoAcoes.ts` 317 linhas); decomposição pura sem mudança de comportamento/aparência. Estado em `EditorItemNucleoEstado.ts`, ações em `EditorItemNucleoAcoes.ts`, sub-componentes de apresentação: BoxAccordion, BoxCanvasPanel, PlacaAccordion, PlacaPanel, PlanoCorte, ResultadoPaineis; helpers e tipos em arquivos próprios. `BoxCanvas.tsx` (já decomposto na R.3b) não foi reaberto. Consumidores confirmados intactos via `graphify explain`: `app/(app)/modulo/page.tsx`, `components/orcamento/ItemEditorTabConectada.tsx`, `components/orcamento/ItemEditorTabMock.tsx` — todos os símbolos importados (`EditorItemNucleo`, `caixaInicial`, `placaInicial`, `ResultadoSalvarItem`) preservados. `code-auditor` APROVADO (1ª tentativa), `qa-engineer` APROVADO (1ª tentativa, 641/641 testes, mesma contagem do baseline). Sem `security-auditor`/`ux-auditor` (refatoração pura, contrato dispensa). Nenhum Circuit Breaker. `frontend-engineer`. Impacto Visual: Nenhum.
- **Task R.3d** ✅ (2026-08-26) — Remover código morto `app/proposta/page.tsx`: página lia `sessionStorage.getItem("proposta")` (nada mais grava desde que a funcionalidade foi descontinuada — achado #2 da Task R.2b), exibia marca antiga "Budget Planner AI". Distinta de `/proposta/[id]/pdf` (rota ativa, não tocada). Removidos `app/proposta/page.tsx` e `app/proposta/proposta.css` (CSS exclusivo dessa página, não compartilhado com `proposta-pdf.css`). Investigação prévia via `graphify explain` + grep confirmou zero import/link remanescente antes de deletar; único vestígio é comentário histórico em `app/(app)/page.tsx`, preservado como está. `code-auditor` APROVADO (1ª tentativa), `qa-engineer` APROVADO (1ª tentativa, 641/641 testes, mesma contagem do baseline, `/proposta/[id]/pdf` confirmada intacta no build). Sem `security-auditor`/`ux-auditor` (remoção de código morto, nenhuma tela ativa tocada, contrato dispensa). Nenhum Circuit Breaker. `frontend-engineer`. Impacto Visual: Nenhum.
- **Task R.4a** ✅ (2026-08-26) — Erradicar CSS legado em `app/modulo/` (RF Design-System §16.4): 15 arquivos migrados de padrões legados (`.card`, `.legado-grid`, `.campos`, `.acoes`, `button.primary/ghost/danger`) para componentes shadcn (`Card`/`CardHeader`/`CardTitle`/`CardContent` — novo primitivo `components/ui/card.tsx`; `Button` com nova variante `outline` para o botão "Avançar"; `Input`/`Select`/`Label` já existentes). Tela-alvo compartilhada: `/modulo` e `/orcamento/[id]/item/[itemId]` (Editor de Item, via `EditorItemNucleo` decomposto na Task R.3c). Botões de avanço que não persistem (Caixa/Divisões/Portas/Gavetas/Puxador/seções de Placa) viraram uniformemente "Avançar" (`variant="outline"`, `ChevronRight`); "Salvar"/"Salvar item" preservado só nos dois pontos reais de persistência. Grid `.campos` → `grid grid-cols-2 gap-md sm:grid-cols-3`. Composição do Editor de Item ajustada: grade `1fr/1fr` (era `.legado-grid` 1.3fr/1fr) e Plano de Corte migrado da coluna de decisão para a coluna de revisão técnica (Poda já prevista em `docs/Screen-Composition.md`). `app/globals.css` intocado (remoção das 59 definições legadas é escopo da Task R.4b). `code-auditor` APROVADO (1ª tentativa), `qa-engineer` APROVADO (1ª tentativa, 641/641 testes, mesma contagem do baseline), `ux-auditor` APROVADO (1ª tentativa, capturas nas duas rotas em 3 breakpoints, varredura de padrão legado zerada, tokens §7.1/7.2/7.9/4/11 conformes). Sem `security-auditor` (mudança de apresentação pura). Nenhum Circuit Breaker — 2 tentativas do executor por estouro de limite de turnos em background sem perda de trabalho na 2ª (progresso preservado via retomada por SendMessage), não conta como reprovação de gate. `frontend-engineer`. Impacto Visual: Completo. **Achado não bloqueante registrado para decisão do operador**: `transition-colors` ausente no hover do card colapsado do accordion (`bg-cinza-50 hover:bg-cinza-100`) — gap pré-existente confirmado anterior a esta task (não introduzido, não tocado pelo diff, fora do escopo de §16.4), aguardando decisão do operador (task futura vs recusa datada) — NÃO arquivar como "candidato" silencioso.
- **Task R.4b** ✅ (2026-08-26) — Erradicar CSS legado do projeto inteiro: escopo ampliado pelo Maestro (+2 consumidores adicionais encontrados antes do contrato) e executor (+1 deletado durante a 2ª retomada). 4 consumidores migrados: `app/configuracoes/materiais/page.tsx` (7 ocorrências, Card/Button/Input/Select/Label/Table shadcn), `app/components/PlanoCorteCanvas.tsx` (`.muted` → `text-cinza-500`), `app/ambientes/page.tsx` (`.wrap` → grid Tailwind), `app/components/LayoutVisualizer.tsx` deletado (código morto confirmado zero import). `app/globals.css` reduzido de ~370 para 63 linhas (todas 59+ definições legadas removidas). `scan-legacy` retorna zero no projeto inteiro. `code-auditor` APROVADO (1ª), `qa-engineer` APROVADO (1ª, 641/641 testes), `ux-auditor` APROVADO (1ª, 6 telas). Nenhum Circuit Breaker — 2 retomadas via SendMessage (1ª por estouro de turnos, 2ª por investigação incompleta do executor corrigida pelo Maestro). `frontend-engineer`. Impacto Visual: Completo. **Fecha a erradicação de CSS legado do projeto inteiro** — nenhuma tela usa padrões antigos. **Pendência operacional registrada**: `ux-auditor` criou usuário/organização de teste real no Supabase (`uxauditor.r4b2.*@maestro.local`) para autenticar em rotas protegidas; considerar limpar esse dado e provisionar `SUPABASE_SERVICE_ROLE_KEY`/`QA_USER_PASSWORD` no futuro.
- **Task R.9** ✅ (2026-08-26) — Remover duplicação de título em Dashboard e Novo orçamento: `h2` interno removido de `components/dashboard/DashboardView.tsx` (`/`) e `components/orcamento/NovoOrcamentoForm.tsx` (`/orcamento/novo`). Título da página já renderizado pela Topbar do shell (`TITULOS_POR_ROTA` em `components/shell/Topbar.tsx`), duplicação eliminada. Nenhuma mudança de layout/comportamento adicional. `code-auditor` APROVADO (1ª tentativa), `qa-engineer` APROVADO (1ª tentativa, 641/641 testes, mesma contagem do baseline), `ux-auditor` APROVADO (1ª tentativa, confirmação visual em 2 breakpoints). Sem `security-auditor` (mudança de apresentação pura). Nenhum Circuit Breaker. `frontend-engineer`. Impacto Visual: Leve.
- **Task R.11** ✅ (2026-08-26) — Transição de hover no card colapsado do accordion: `transition-colors duration-150` (Design-System §12) adicionado ao hover `bg-cinza-50 → hover:bg-cinza-100` nos 10 arquivos `app/modulo/*Card.tsx` (CaixaCard, DivisoesCard, GavetasCard, PlacaBordaCard, PlacaDimensoesCard, PlacaEngrossamentoCard, PlacaOrientacaoCard, PlacaRipadoCard, PortasCard, PuxadorCard). Achado não bloqueante da Task R.4a (`ux-auditor`), decisão do operador (2026-08-26): vira task, não recusa datada. Correção pontual, sem mudança estrutural. `code-auditor` APROVADO (1ª tentativa), `qa-engineer` APROVADO (1ª tentativa, 641/641 testes), `ux-auditor` APROVADO (1ª tentativa, transição confirmada por amostragem de cor em 3 pontos no tempo via Playwright — 248→243→241, interpolação real). Sem `security-auditor`. Nenhum Circuit Breaker. `frontend-engineer`. Impacto Visual: Leve.
- **Task R.5a** ✅ (2026-08-27) — Composição e acabamento terminal — Orçamento (`/orcamento/[id]`): tela fechada no nível `release` com composição conforme `docs/Screen-Composition.md`. Processo com 3 rodadas de `art-director` (rodada 1: REPROVADO — 4 achados: canto morto, items-stretch, densidade invertida, teste do vulto; rodada 2: REPROVADO — 1 achado: 2 massas accent na aba Proposta; Circuit Breaker acionado após 2 reprovações consecutivas, autorização explícita do operador para rodada 3; rodada 3: APROVADO). Correções aplicadas: um único sistema de título/botão/campo/card, quatro estados no mesmo nível de acabamento, `scan-legacy` zerado. Code-auditor/qa-engineer/ux-auditor APROVADO na rodada final (5ª total) sem regressão. Product-designer resolveu 4 observações de design não bloqueantes durante o processo (domínio "Domina" por região, densidade uniforme entre abas, painel inline transitório, contradição do card "Preço final"). Dois achados não bloqueantes da rodada final ainda pendentes de decisão do Maestro/operador (A: padding de card sem variante mobile, sistêmico/pré-existente; B: DS §7.11a com condição de exclusividade em LinhaPropostaCard, correção de 2 linhas) — não foram resolvidos, aguardando decisão sobre virarem task ou recusa datada. `frontend-engineer`. Impacto Visual: Completo.
- **Task R.5b** ✅ (2026-08-27) — Composição e acabamento terminal — Editor de Item (`/orcamento/[id]/item/[itemId]`): tela fechada no nível `release` com composição conforme `docs/Screen-Composition.md`. Processo com 2 tentativas do `art-director` (tentativa 1: REPROVADO — 4 achados: KPI "Preço final" tintado, vazio da tabela "Peças" ausente, canto morto de 1443px na coluna esquerda, ação primária fora do rodapé real da coluna — todos corrigidos; tentativa 2: APROVADO). Correções introduziram 2 regressões pegas pelo `ux-auditor`: sticky não funcionava de verdade (causa raiz corrigida em `components/shell/Shell.tsx`, arquivo compartilhado por todas as telas — `overflow-x-hidden` → `overflow-x-clip`) e transbordamento mobile do KPI (ambas corrigidas antes da rodada final). Code-auditor/qa-engineer/ux-auditor APROVADO na rodada final sem regressão (641 testes). Art-director confirmou por MD5 idêntico de screenshot que mudança em Shell.tsx não teve efeito colateral visual em outras telas (Dashboard). 7 observações não bloqueantes do art-director (6 da tentativa 1 + 1 nova: "gagueira de copy" — dois estados vazios com título idêntico "Nenhuma peça gerada ainda") pendentes de decisão (não foram resolvidas, destino: product-designer). `frontend-engineer`. Impacto Visual: Completo.

**Lote 4 — Todas as 13 tasks concluídas e mescladas (13/13)**:
- **Task 4.4** ✅ (2026-08-12) — Coluna `codigo` (text, nullable) em `produto` com constraint `unique (organizacao_id, codigo)`. Migration `20260812120000_produto_codigo.sql`. Tipos em `lib/produto/tipos.ts`, leitura/escrita em `lib/produto/acoes.ts` (`criarProduto`/`atualizarProduto`) com tratamento de erro de violação de unicidade (Postgres `23505`) → mensagem amigável (`mensagemErroProduto`). Três testes novos em `lib/produto/acoes.test.ts`. `code-auditor` APROVADO (1ª), `qa-engineer` APROVADO (1ª, 566/566 testes). Sem `security-auditor` (RLS de `produto` já existente cobre a tabela). Sem `ux-auditor` (backend puro). **Migration real NÃO aplicada em Supabase** — bloqueio CLI; pendente operador. **Desbloqueia Task 4.1–4.3/4.5 (Catálogo unificado).**
- **Task 4.1–4.3, 4.5** ✅ (2026-08-12) — Catálogo unificado (RF-30): elimina Tabs por categoria, passa a card único com seletor de categoria interno (Select), botão genérico "Adicionar item" (nunca "Adicionar chapa"), `FormularioProdutoDialog` parametrizado por categoria (campos específicos só depois de escolhida a categoria: chapa=cor+espessura+textura, ferragem=código fixo+nome auto-preenchido, fita/LED/acessorio=nome+preço+unidade), Campo universal `codigo` exposto na UI em todas categorias, Status preservado. Categoria read-only em edição de produto existente. 6 arquivos antigos removidos, substituídos por `FormularioProdutoDialog.tsx` + `TabelaProdutos.tsx` (líquido -387 linhas). `code-auditor` APROVADO (1ª), `qa-engineer` APROVADO (1ª, 575/575 testes, 9 novos por categoria), `ux-auditor` APROVADO (1ª, 11 screenshots, 3 breakpoints, Design-System conformes). 2 observações não bloqueantes: (1) modo escuro gap pré-existente; (2) estado vazio não capturável no harness. Impacto Visual: Completo. Sem migration (UI pura).
- **Task 4.16 (back)** ✅ (2026-08-11) — Coluna `espessura_serra_padrao_mm numeric` em `organizacao` (default `3`, check `>= 0`), leitura/escrita no perfil. Round-trip mínimo, sem UI. `code-auditor` APROVADO (1ª), `qa-engineer` APROVADO (1ª, 452/452 testes). Sem impacto visual. Migration real não aplicada em Supabase (bloqueio CLI); pendente operador. **Desbloqueou a Task 3.1/3.3 (motor), Lote 3.**
- **Task 4.8–4.9 (back)** ✅ (2026-08-12) — Upload de logo: bucket de Storage privado `organizacao-logos` (2 MB, PNG/JPEG/WEBP, sem SVG), 4 políticas de RLS escopadas por organização, módulo `lib/organizacao/logo-storage.ts` com path determinístico `{organizacao_id}/logo.<ext>`, upload client-side com `upsert: true`, leitura via signed URL sob demanda. Coluna `organizacao.logo_url` reaproveitada (muda de semântica), comentários atualizados em `lib/organizacao/salvar.ts`, `lib/perfil/carregar.ts`, `lib/proposta-pdf/carregar.ts`. `code-auditor` APROVADO (1ª), `security-auditor` APROVADO (1ª real, isolamento cross-tenant confirmado, sem segredo exposto), `qa-engineer` APROVADO (1ª, 580/580 testes, 5 novos). Sem `ux-auditor` (backend puro). **Migration real NÃO aplicada em Supabase** — bloqueio CLI; pendente operador. **Desbloqueia Task 4.8–4.9 (front) e Task 4.10.**
- **Task 4.8–4.9 (front)** ✅ (2026-08-12) — UI de upload de logo em `/perfil` (RF-31): campo de texto "Logo (URL da imagem)" removido de `SecaoOrganizacao` em `components/perfil/PerfilLab.tsx`, substituído por controle de upload de arquivo real (`input type="file" accept="image/png,image/jpeg,image/webp"` oculto, disparado por `Button`), consumindo `lib/organizacao/logo-storage.ts` (Task 4.8–4.9 back). Upload dispara imediatamente na seleção (loading "Enviando…"), validação client-side de mime/tamanho via função pura `validarArquivoLogo` (testada), preview via signed URL com fallback para URL externa legada e placeholder `ImageOff` para vazio. Persistência do path no clique de "Salvar alterações" (fluxo de dois passos igual aos demais campos). `code-auditor` APROVADO (1ª), sem `security-auditor` (RLS já protege desde Task 4.8–4.9 back), `qa-engineer` APROVADO (1ª, 584/584 testes, 4 novos de mime/tamanho), `ux-auditor` APROVADO (1ª, 6 screenshots: vazio/loading/erro capturados; "preenchido" não capturável em harness por exigir sessão autenticada — observação não bloqueante). Impacto Visual: Leve. Sem migration. **Desbloqueia Task 4.10 (fallback de marca padrão).**
- **Task 4.10** ✅ (2026-08-12) — Fallback de marca padrão no PDF de proposta: `lib/proposta-pdf/carregar.ts` ganhou `assinarUrlLogo` (réplica de `assinarUrlImagem` já existente no mesmo arquivo), resolvendo `organizacao.logo_url` (path de Storage desde Task 4.8–4.9 back) em signed URL no servidor, antes do primeiro paint. `logo_url` nulo ou path que falhe ao resolver vira `null`, deixando o fallback visual já existente em `PropostaPdfCabecalho.tsx` assumir (`?? "/logo/logo-dark.png"`). Nenhuma mudança de componente visual, só camada de dados server-side. `code-auditor` APROVADO (1ª), sem `security-auditor` (sem RLS/política nova), `qa-engineer` APROVADO (1ª, 587/587 testes, 3 novos cobrindo path válido/nulo/inválido), `ux-auditor` APROVADO (1ª, screenshot confirmando marca padrão com `logoUrl: null`; "com logo real" não capturável sem sessão — observação não bloqueante). Impacto Visual: Leve. Sem migration. **Fecha RF-31 (upload de logo com fallback de marca padrão) ponta a ponta.**
- **Task 4.6–4.7** ✅ (2026-08-13) — Máscara de CNPJ e telefone em `/perfil` (RF-31): funções puras `formatarCnpj`/`formatarTelefone` novas em `lib/format.ts`, aplicadas via `onChange` nos 3 campos `org-cnpj`, `org-telefone`, `perfil-telefone` de `components/perfil/PerfilLab.tsx`. Máscara progressiva, lida com string suja, campo vazio permanece vazio, telefone troca automaticamente entre formato fixo (10 dígitos) e celular (11 dígitos). Sem mudança de Server Action/schema. `code-auditor` APROVADO (1ª), `qa-engineer` APROVADO (1ª, 13 testes escopados), `ux-auditor` APROVADO (1ª, screenshot via `/dev/preview/perfil` confirmando os 3 campos: CNPJ, telefone celular 11 dígitos, telefone fixo 10 dígitos). Impacto Visual: Leve. Sem migration.
- **Task 4.11 (back)** ✅ (2026-08-13) — Upload de foto de perfil pessoal: bucket de Storage privado `perfil-fotos` (2 MB, PNG/JPEG/WEBP, sem SVG), migration `supabase/migrations/20260813140434_perfil_foto_url_storage.sql` com coluna `perfil.foto_url text nullable` e 4 políticas de RLS escopadas por usuário individual (`auth.uid()`, não por organização — colegas da mesma organização não podem ler/escrever a foto uns dos outros), módulo `lib/perfil/foto-storage.ts` (novo, réplica de `lib/organizacao/logo-storage.ts`) com path determinístico `{perfil_id}/foto.<ext>`, upload client-side com `upsert: true`, leitura via signed URL sob demanda. Coluna `PerfilCarregado.fotoUrl`/`DadosPerfil.fotoUrl` adicionadas em `lib/perfil/carregar.ts`/`lib/perfil/salvar.ts` (mesmo `.update()` já existente, sem Server Action nova). Ajustes mínimos em `PerfilMock.tsx`/`app/(app)/perfil/page.tsx` só para satisfazer o tipo (`fotoUrl: ""`), nenhuma UI nova. `code-auditor` APROVADO (1ª, veredito em `.maestro/tmp/verdicts/4.11-back-code-auditor.md`), `security-auditor` APROVADO (1ª, isolamento escopado por `auth.uid()` confirmado — não depende de `perfilId` enviado pelo client, só da RLS avaliada contra sessão real, veredito em `.maestro/tmp/verdicts/4.11-back-security-auditor.md`), `qa-engineer` APROVADO (1ª, 601/601 testes, 5 novos cobrindo path determinístico/upsert/isolamento/rejeição de SVG, veredito em `.maestro/tmp/verdicts/4.11-back-qa-engineer.md`). Sem `ux-auditor` (backend puro, sem UI). **Migration real NÃO aplicada no Supabase** — bloqueio de permissão de conta CLI; pendente operador via MCP/Dashboard. **Desbloqueia Task 4.11 (front).**
- **Task 4.11 (front)** ✅ (2026-08-13) — UI de upload de foto de perfil pessoal em `/perfil` (RF-31): `SecaoPerfilPessoal` (`components/perfil/PerfilLab.tsx`) ganha controle de upload de arquivo, réplica fiel do padrão já aprovado em `SecaoOrganizacao` (Task 4.8–4.9 front) — upload dispara na seleção com validação client-side (`validarArquivoFoto`, mesmos limites de mime/tamanho de `validarArquivoLogo`), estado de loading, preview via signed URL (`obterUrlAssinadaFotoPerfil`), path persistido só no clique de "Salvar alterações". `PerfilCarregado` ganhou o campo `id` (populado em `carregarPerfilOrganizacao` a partir de `user.id`, já obtido ali) para que `SecaoPerfilPessoal` monte o path de upload sem uma segunda leitura de sessão no client — mesmo espírito de `organizacaoInicial.id`. `SecaoOrganizacao` não foi tocada. `code-auditor` APROVADO (1ª tentativa, veredito em `.maestro/tmp/verdicts/4.11-front-code-auditor.md`). Sem `security-auditor` (upload já protegido por RLS desde a Task 4.11 back). `qa-engineer` APROVADO (1ª tentativa, 605/605 testes, 4 novos cobrindo validação de mime/tamanho, veredito em `.maestro/tmp/verdicts/4.11-front-qa-engineer.md`). `ux-auditor` APROVADO (1ª tentativa, 5 screenshots via `/dev/preview/perfil` confirmando estados vazio/erro/loading e `SecaoOrganizacao` visualmente intocada, veredito em `.maestro/tmp/verdicts/4.11-front-ux-auditor.md`). Impacto Visual: Leve. Sem migration. **Fecha RF-31 (foto de perfil pessoal) ponta a ponta** — junto com a Task 4.11 (back).
- **Task 4.12–4.13 (back)** ✅ (2026-08-13) — Troca de senha com confirmação por e-mail (RF-31, fluxo Supabase Auth): rota `app/auth/confirm/route.ts` (pré-existente) estendida para `type=recovery` além de `type=email`, redireciona para `/perfil?definirSenha=1` em vez de `/` em recuperação, mantendo `type=email` sem regressão. Duas Server Actions novas em `lib/auth/trocar-senha.ts`: `solicitarTrocaSenha()` (sem parâmetro, e-mail vem de `getUser()`, chama `resetPasswordForEmail`), `definirNovaSenha(novaSenha)` (valida com `senhaValida` reaproveitada de `lib/auth/validacao.ts`, chama `updateUser({password})`). Nova `mensagemErroTrocaSenha` em `lib/auth/mensagens.ts`, padrão de `mensagemErroLogin`/`mensagemErroSignup`. Nenhuma UI tocada. `code-auditor` APROVADO (1ª tentativa, veredito em `.maestro/tmp/verdicts/4.12-4.13-back-code-auditor.md`; achado não bloqueante: executor escreveu em `docs/STATUS.md` território de memory-manager, conteúdo correto). `security-auditor` APROVADO escalado para Opus por superfície de autenticação (veredito em `.maestro/tmp/verdicts/4.12-4.13-back-security-auditor.md`), análise extensa sem achado bloqueante mas com 4 observações registradas para decisão futura: (O1) `redirectTo` sem allowlist — inofensivo hoje (exige sessão da vítima), frágil se fluxo futuro "esqueci minha senha" anônimo reaproveitar (mitigação: allowlist se fluxo anônimo vier); (O2) `definirNovaSenha` aceita qualquer sessão válida, não só a de recuperação — decisão de contrato, Supabase oferece reautenticação recente no dashboard (recomendado ligar se gestão de segurança escopado vir); (O3) `type` sem validação — pré-existente, inofensivo; (O4) fallback de protocolo não cobre `127.0.0.1` — conveniência local, não segurança. `qa-engineer` APROVADO (1ª tentativa, 609/609 testes, 4 novos de `mensagemErroTrocaSenha`, veredito em `.maestro/tmp/verdicts/4.12-4.13-back-qa-engineer.md`). Sem `ux-auditor` (backend puro, sem UI). Sem migration. **Desbloqueia a Task 4.12–4.13 (front).**
- **Task 4.12–4.13 (front)** ✅ (2026-08-13) — Segurança e troca de senha (RF-31): nova seção `SecaoSeguranca` em `components/perfil/PerfilLab.tsx` replicando padrão de `SecaoOrganizacao`/`SecaoPerfilPessoal`, campo de e-mail read-only com aviso de "não editável — identificador da conta". **E-mail movido** de `SecaoPerfilPessoal` para `SecaoSeguranca` (refactoring de domínio: e-mail é identidade de conta, não dado pessoal). Botão "Trocar senha" chama `solicitarTrocaSenha()` (Task back, já mesclada) com confirmação inline via `Dialog`. Formulário "Definir nova senha" (dois campos + validação client-side reaproveitando `senhaValida`/`senhasConferem`/`TAMANHO_MINIMO_SENHA` de `lib/auth/validacao.ts`, sem duplicação de regra) renderizado só quando `?definirSenha=1`, propagado via `searchParams` do Server Component `app/(app)/perfil/page.tsx` até `SecaoSeguranca` sem segunda leitura de sessão no client. Ao confirmar, `router.replace("/perfil")` remove parâmetro e limpa estado. `SecaoOrganizacao` não foi tocada. `code-auditor` APROVADO (1ª, veredito em `.maestro/tmp/verdicts/4.12-4.13-front-code-auditor.md`). Sem `security-auditor` (consumo de Server Actions já auditadas na Task back, sem lógica de autenticação nova no client). `qa-engineer` APROVADO (1ª, 609/609 testes, sem regressão — task é wiring de UI sem função pura nova, veredito em `.maestro/tmp/verdicts/4.12-4.13-front-qa-engineer.md`). `ux-auditor` APROVADO (1ª, 3 screenshots via `/dev/preview/perfil` + `?definirSenha=1`, Design-System conformes, 3 estados confirmados, veredito em `.maestro/tmp/verdicts/4.12-4.13-front-ux-auditor.md`). Impacto Visual: Leve. Sem migration. **Fecha RF-31 (troca de senha com confirmação por e-mail) ponta a ponta** — junto com a Task 4.12-4.13 (back).
- **Task 4.15** ✅ (2026-08-13) — Exclusão de conta = exclusão de organização inteira (RF-31, Q-13/Q-17): Server Action `excluirOrganizacao` em `lib/organizacao/excluir.ts` com checagem de papel admin em primeiro lugar (E-C3/`NAO_AUTORIZADO_EXCLUIR_ORG`/403 antes de qualquer delete), migration `orcamento.cliente_id` de RESTRICT para NO ACTION (Modelo 7.3), leitura de perfis antes de delete (rastreamento para Admin API — passo 2), delete em cascata de `organizacao` (12 tabelas: perfil/cliente/produto/gabarito/orcamento + ambiente/parede/linha_proposta/lista_material/elemento_continuo/elemento_parede_preset via cascade), expurgo de Storage sob prefixos `{organizacao_id}/*` em 3 buckets (organizacao-logos, perfil-fotos, linha-proposta-renders — este terceiro foi adicionado durante veto de segurança), remoção de usuários em `auth.users` via Admin API (`service_role`, reutilizando `@supabase/supabase-js` já instalado). Dialog de confirmação com padrão reforçado de Design-System §7.11 (campo digitação nome exato da organização + botão sólido vermelho + rótulo "Excluir organização"). **2 rodadas de gate**: `security-auditor` (Opus) reprovou 1ª tentativa — coluna `perfil.papel` era gravável pelo próprio usuário via PostgREST, permitindo auto-promoção a admin; corrigido com `revoke(select, insert, update, delete) on public.perfil (papel) from authenticated` e `grant(select) on public.perfil (papel) to authenticated` (leitura sim, escrita não — checagem de papel fica server-only em Server Actions). `ux-auditor` reprovou 1ª tentativa — Dialog usava padrão simples de confirmação em vez do padrão reforçado da §7.11; corrigido com campo de digitação, botão sólido bg-erro, rótulo exato "Excluir organização" (wiring em `components/perfil/ExcluirOrgDialog.tsx`). `code-auditor` APROVADO, `security-auditor` APROVADO (2ª), `qa-engineer` APROVADO, `ux-auditor` APROVADO (2ª). Impacto Visual: Leve. **Nenhum Circuit Breaker ativado.** Observação não bloqueante registrada para task futura: para usuários não-admin (cenário hoje inalcançável — toda org tem 1 admin obrigatório), o Design-System recomenda botão disabled + Tooltip em vez de desaparecer, mas nenhum primitivo Tooltip existe no projeto; decisão de implementar fica em backlog.
- **Task 4.16 (front)** ✅ (2026-08-14) — Campo "Espessura de serra (kerf)" em `/perfil` (`SecaoOrganizacao`): `<Input type="number" min={0}>` editável, função pura `sanitizarKerf` (novo em `components/perfil/PerfilLab.tsx:209`, exportada) valida 0 como válido, rejeita negativo/NaN → 0, legenda de aviso de retroatividade (mudar valor altera plano de corte de orçamento não congelado). 4 testes novos em `components/perfil/PerfilLab.test.ts:83-101` (`describe("sanitizarKerf", ...)`). `code-auditor` APROVADO (1ª), `qa-engineer` APROVADO (1ª, 633/633 testes), `ux-auditor` APROVADO (1ª, 2 screenshots). Impacto Visual: Leve. **Fecha RF-02/RF-34 (kerf) ponta a ponta** — junto com Task 4.16 (back). **Fecha o Lote 4 por completo — 13/13 tasks concluídas**.

Histórico das demais tarefas do Lote 2 (17/17 concluídas):
- **Task 2.1** ✅ (2026-08-06) — Investigação: caminho de "criar módulo do zero" já existe e funciona ponta a ponta (`/biblioteca` → botão "Novo módulo" → `/modulo` sem `?preset=` → salvar chama `criarGabarito` já existente → volta e aparece na listagem). Sidebar deliberadamente exclui `/modulo` do menu principal (Task 13.3b). Sem mudança de código necessária. Nenhuma branch de merge.
- **Task 2.1 (dedup)** ✅ (2026-08-06) — `deduplicarPromovidos()` em `lib/gabarito/listar.ts`: filtro derivado em memória escondendo gabaritosglobais promovidos da própria organização. 3 testes novos em `lib/gabarito/listar.test.ts`. `code-auditor` APROVADO (veredito em arquivo, build/lint/typecheck limpos), `qa-engineer` APROVADO (veredito em arquivo, 370 testes, 3 critérios). Sem RLS nova. Merge `--no-ff` em `main`.
- **Task 2.13** ✅ (2026-08-06) — Rename "Preset" → "Módulo" em toda a UI. Identificadores de código mantidos intactos. `code-auditor` APROVADO, `qa-engineer` APROVADO. Impacto Visual: Nenhum.
- **Task 2.8–2.11 (back)** ✅ (2026-08-06) — Backfill de `id`/`refX`/`refY`/`nome` em `ElementoParede` via migração de leitura. Sem migration SQL. `code-auditor` APROVADO, `qa-engineer` APROVADO (26 testes do domínio).
- **Task 2.7** ✅ (2026-08-06) — Motor: tipo `"pedra"`, bug fix em `existeElementoBloqueante`, aviso `TAMPO_SOBRE_PEDRA`. `code-auditor` APROVADO, `qa-engineer` APROVADO. Conflito de merge resolvido pelo motor-engineer.
- **Task 2.12 (back)** ✅ (2026-08-06) — Tabela `elemento_parede_preset`, RLS, domínio completo. `code-auditor` APROVADO, `security-auditor` APROVADO. Migration não aplicada em Supabase real (bloqueio de permissão de CLI).
- **Task 2.7-2.11 (front)** ✅ (2026-08-06) — UI de elemento de parede: tipo "Pedra" + edição inline convergente (lápis e clique 2D no mesmo estado) + seletores de referência de medida com rótulos descritivos. `code-auditor` APROVADO, `qa-engineer` APROVADO. **`ux-auditor` não executado (autorizado pelo operador em 2026-08-06)** — auditoria visual em screenshots confirmou critérios atendidos; veredito não gravado por bug known de transporte (gate indisponível, não reprovação). Achado incidental registrado para Task 5.5–5.6 (Lote 5).
- **Task 2.12 (front)** ✅ (2026-08-06) — UI de criar/listar/aplicar preset com threading via `AmbientesTabConectada`. Retrofit: campo "Nome (opcional)" adicionado ao formulário-base (gap descoberto na 1ª tentativa, type existia mas faltava input). `code-auditor` APROVADO, `qa-engineer` APROVADO, `ux-auditor` APROVADO. Impacto Visual: Leve.
- **Task 2.18 (motor)** ✅ (2026-08-06) — Posicionamento por vão até o vizinho: funções `calcularVizinhos`, `converterVaoParaX`, `converterXParaVao` em `lib/engine/parede/posicionamento.ts` (novo, 13 testes). Campo `refEntrada` em `ItemPosicionado`. Todos os 4 exemplos do Modelo 3.1.1 reproduzidos com valores exatos. Torre como vizinha cross-faixa. `code-auditor` APROVADO, `qa-engineer` APROVADO. Desbloqueia Task 2.18 (front).
- **Task 2.3–2.6** ✅ (2026-08-07) — Cadastro/edição/ordenação de múltiplos ambientes e paredes, seletor com indicação visual permanente de qual ambiente/parede está em edição (não depende de hover). `code-auditor` APROVADO (1ª tentativa, 425/425 testes), `security-auditor` APROVADO (2ª tentativa, cross-tenant corrigido), `qa-engineer` APROVADO (1ª tentativa, 426/426 testes, 8 critérios), `ux-auditor` NÃO EXECUTADO (autorizado pelo operador em 2026-08-07 — 1ª tentativa reprovou por bug CSS legado, corrigido; reconvocações posteriores rodaram com screenshots reais mas arquivo de veredito não gravou por bug de transporte, operador aprovouvisualmente). Impacto Visual: Completo.
- **Task 2.3–2.6 (alturas)** ✅ (2026-08-07) — UI de override de altura por parede em `AmbientesLab.tsx`: indicador "herdado"/"customizado" derivado, botão "Voltar ao herdado" que remove override. Aviso de propagação em `/perfil`. Funções puras `definirAlturaOverride`/`removerAlturaOverride` exportadas e testadas. `code-auditor` APROVADO (2ª tentativa), `qa-engineer` APROVADO (2ª tentativa, 445/445 testes), `ux-auditor` APROVADO (1ª tentativa, checklist §15.4, Impacto Visual Leve). Impacto Visual: Leve.
- **Task 2.18 (front)** ✅ (2026-08-08) — UI de inserção com campo de vão: substitui "X (mm)" por "Vão (mm)" com Select "Referência", conversão vão↔X via `converterVaoParaX`/`converterXParaVao`. Tabela "Itens posicionados" com colunas "Vão esq."/"Vão dir." recalculadas a cada render. `code-auditor` APROVADO (1ª tentativa), `qa-engineer` APROVADO (1ª tentativa, 445/445 testes), `ux-auditor` APROVADO (1ª tentativa, checklist §15.4, Impacto Visual Leve, 2 observações não bloqueantes para product-designer em task futura). Impacto Visual: Leve.
- **Task 2.19–2.23** ✅ (2026-08-08) — Bloco "Personalizar módulo" no formulário de inserção: 9 campos (largura/altura/profundidade, cor+espessura da caixa, cor+espessura das portas —ocultos sem `GrupoPortas`—, fundo, puxador), repopulados via `useEffect` a cada módulo. Spread não-mutativo sobre `preset.box`. `code-auditor` APROVADO (1ª), `qa-engineer` APROVADO (1ª, 445 testes), `ux-auditor` APROVADO (1ª, §15.4, Impacto Visual Leve). Impacto Visual: Leve.
- **Task 2.14–2.17** ✅ (2026-08-08) — Seleção em cascata ambiente → faixa → módulo: 2 itens novos (cascata real, rótulo "Meio"), 2 já implementados em tasks anteriores (confirmados sem reimplementação). `code-auditor` APROVADO (1ª), `qa-engineer` APROVADO (1ª, 445 testes), `ux-auditor` APROVADO (1ª, §15.4, Impacto Visual Leve). Achado incidental: mensagem de validação cita `"bancada"` — candidato a task futura. Impacto Visual: Leve.
- **Task 2.31** ✅ (2026-08-08) — Botão "Cancelar divisão" em `LinhaPropostaCard.tsx`: reverte divisão, devolve itens para linha mãe (rastro de origem efêmero em `origemSplit` React state). Trata mãe já excluída sem quebra. Reutiliza Server Actions existentes. `code-auditor`/`qa-engineer`/`ux-auditor` APROVADO (1ª tentativa cada). Impacto Visual: Leve.
- **Task 2.32** ✅ (2026-08-08) — Função pura `ambientesDaLinha` (`lib/linha-proposta/ambientes.ts`, 4 testes) deriva nomes dos ambientes de uma Linha de Proposta. Caption "Ambiente: {nome(s)}" em `LinhaPropostaCard.tsx`, recalculado do estado atual. Vínculo nunca se perdeu nos dados, só não era exibido; sobrevive a dividir/mesclar/renomear. `code-auditor`/`qa-engineer`/`ux-auditor` APROVADO (1ª cada). Achado incidental: `carregar.ts` cria linha padrão cobrindo TODOS os ambientes — decisão de produto pendente. Impacto Visual: Leve.
- **Task 2.24–2.26** ✅ (2026-08-08) — Elevação da parede passa a desenhar os módulos posicionados: item 2.25 (rótulo "Meio") já estava pronto, confirmado sem reimplementação. Novos: 2.24 (função `retangulosDosItens` deriva geometria via `derivarY`/`larguraDoItem`/`alturaDoItem` existentes) e 2.26 (torre desenhada com retângulo real, primeiro na ordem SVG, módulos das demais faixas por cima em sobreposição). `code-auditor` APROVADO (1ª), `qa-engineer` APROVADO (1ª, 452 testes), `ux-auditor` APROVADO (1ª real — 1ª rodada auditoria completa mas veredito não gravou por bug transporte; reconvocada sem contar reprovação; §15.4 com desktop/tablet/mobile). Impacto Visual: Completo.
- **Task 2.27** ✅ (2026-08-14) — Cotas técnicas à direita da elevação (altura total + altura de cada faixa): função pura `cotasFaixas(bandas)` em `components/ambientes/ElevacaoParede.tsx`, interface `CotaFaixa`, 4 testes (3 valores da fixture: 900/500/1300mm, caso de borda lista vazia). `MARGIN_RIGHT` do SVG alargado (56 → 100), bracket da torre deslocado. Estilo: linha cinza-400, seta, texto tabular-nums cinza-700 (Design-System §9.5). Sem prop nova, sem schema novo. `code-auditor` APROVADO (1ª, veredito em arquivo), `qa-engineer` APROVADO (1ª, 626 testes, sem regressão), `ux-auditor` APROVADO (1ª, 3 colunas sem sobreposição via `/dev/preview/orcamento`, conformidade §9.5). Sem `security-auditor` (design puro). Impacto Visual: Leve.
- **Task 2.28–2.30** ✅ (2026-08-14) — Agrupamento comercial cross-faixa/parede com afordância visual distinta: investigação (executor + code-auditor + qa-engineer) confirmou 2.28/2.29/2.30 atendidos por código pré-existente. Gap real: badge comercial somente-leitura na aba Ambientes mostrando vínculo de item a `LinhaProposta`, cor `accent` (#B45309, distinto de handle de Conjunto `informacao` #2563EB). Badge com 2+ linhas (padrão D-17, sem redundância). Função `derivarTagsComerciais` (3 casos testados), prop `tagsComerciais` em `BoxCanvas`/`ElevacaoParede` (opcional). Plumbing reaproveitando `carregarOuCriarLinhasProposta` (sem schema novo), harness mock 2+ linhas. `code-auditor` APROVADO, `qa-engineer` APROVADO (629 testes, sem regressão), `ux-auditor` APROVADO (4 screenshots). Nenhum Circuit Breaker. Uma retomada via SendMessage. Impacto Visual: Completo. Sem migration. **Fecha o Lote 2 por completo — 18/18 tasks.**

**Lote 1 (Confiança e estado) — Histórico (7/7 tasks concluídas):**
- **Task 1.1–1.3** ✅ (2026-08-04) — Invalidação de cache após mutação + aba persistida na URL + "atualizar render" sem F5. `code-auditor` indisponível (2x sem veredito, estouro técnico) — revisão assumida pelo Maestro. `qa-engineer` aprovou com veredito próprio (verificação real: aba sobrevive a F5, `forceMount` e `router.refresh()` validados). Sem impacto visual.
- **Task 1.8** ✅ (2026-08-04) — Investigação: link "← calculadora" em `/modulo` já aponta para `/` (Dashboard), destino mais adequado. Sem mudança de código. Investigação factual verificada antes do merge.
- **Task 1.5–1.6** ✅ (2026-08-05) — Teste automatizado de paridade financeiro ↔ proposta. Causa raiz: `FinanceiroTabConectada.tsx` faltava `router.refresh()` após `salvarConfiguracaoPrecificacao`. `code-auditor` indisponível (estouro técnico) — revisão assumida pelo Maestro (lint/typecheck/test + diff relido). `qa-engineer` aprovado (5/5 critérios). Sem impacto visual.
- **Task 1.7** ✅ (2026-08-05) — Bug de chapas de 6 mm em "valor por chapa": `consolidarResultados` derivava contagem de chapas por fórmula de área, desacoplada do bin-packing real. Correção: fonte única de verdade do `planoDeCorte`. `code-auditor` indisponível (estouro técnico) — revisão assumida pelo Maestro (lint/typecheck/test + diff relido, 348 testes). `qa-engineer` aprovado (5/5 critérios, reprodução do bug confirmada). Sem impacto visual.
- **Task 5.10 (back)** ✅ (2026-08-05, Lote 5) — Coluna `orcamento.etapa_esteira` (migration aplicada no Supabase real), Server Action `atualizarEtapaEsteira` (transições T1–T3), gatilho de avanço automático para `aguardando_aprovacao` no congelamento. `code-auditor` aprovado (veredito em arquivo), `qa-engineer` aprovado (358 testes, 5 critérios). Sem RLS nova, sem UI. Desbloqueia a Task 1.9 (back/front).
- **Task 1.9-back** ✅ (2026-08-05) — Server Action `reabrirOrcamento` com autorização por papel (`admin` obrigatório), congelamento revertido, etapa avançada quando aplicável. `code-auditor` aprovado, `security-auditor` aprovado (gate obrigatório para autorização por papel), `qa-engineer` aprovado (364 testes, 6 novos). Sem impacto visual.
- **Task 1.9-front** ✅ (2026-08-06) — Botão "Reabrir orçamento" no Alert de congelamento, visível/habilitado só para `admin`, abre Dialog de confirmação, liga à Server Action `reabrirOrcamento`. Bug de causa raiz corrigido em `app/globals.css` (colisão de classe `.grid` legada com utilitário Tailwind `grid`, quebrava DialogContent em desktop). `code-auditor` aprovado, `qa-engineer` aprovado (367 testes), `ux-auditor` aprovado na 2ª tentativa. Impacto Visual: Leve.

Fases A–C + Task 0.1–0.3: ver histórico em `docs/Backlog.md` ("Resumo do
Épico V2" e "Lote 0 — Fundação de dados").

## 2. Como se orientar (leia nesta ordem)

1. **`docs/PRD.md`** — visão, persona, jornada, requisitos, decisões D-01 a
   D-26 (todas fechadas).
2. **`docs/Modelo-de-Dominio.md`** — a fundação técnica: `ItemOrcamento`
   union (`BoxModule | Placa`), Parede/Ambiente/Conjunto, elementos
   contínuos, rateio de preço, veio de chapa.
3. **`docs/Mapa-de-Telas.md`** — a árvore de telas (`/orcamento/[id]` com
   abas), derivada do modelo de domínio.
4. **`docs/Design-System.md`** — tokens de cor/tipografia/espaçamento (v3,
   sidebar navy + laranja). Não inventar valor visual fora daqui.
5. **`docs/Backlog.md`** — o que ainda não foi feito (dívida de segurança
   Stage 3, avaliações de upgrade Stage 4, ideias pós-MVP).
6. **`.maestro/agents/maestro.md`** e `.maestro/` — o framework de
   orquestração (branch efêmera → gates → merge `--no-ff` em `main`).
7. `docs/archive/` — planejamento original V1, só para curiosidade
   histórica. Não é fonte de verdade.

## 3. O que existe e funciona

**Motor de cálculo** (`lib/engine/*`, `lib/orcamento.ts`) — único motor (V1
removido por completo): explosão de `BoxModule`/`Placa` em peças, bin-packing
com restrição de veio, `Parede`/`Ambiente`/`Conjunto`/`ElementoContinuo`
(tampo/rodapé/tamponamento/fechamento unificados), precificação V2 (4 modos
de preço + 3 de montagem, rateio por custo alocado, `RateioSnapshot`
congelável). `pricing.ts` V1 segue coexistindo intocado (consumido só como
fallback/preview em `/modulo`).

**Persistência e Auth** — Supabase real (`orcamentofacil`,
`ioakptuwhfvlirvrciwg`). Multi-tenant por `organizacao` via RLS
(`private.org_do_usuario()`), auth por Supabase Auth (`/login`, `/signup`,
gate deny-by-default no `middleware.ts`). Tabelas: `organizacao`, `perfil`,
`cliente`, `produto`, `gabarito`, `orcamento`, `ambiente`, `parede`,
`elemento_continuo`, `linha_proposta`, `lista_material` — todas com RLS
própria. Teste de isolamento por tenant em
`supabase/tests/isolamento-tenant.sql` (rodar de novo após qualquer RLS
nova). Storage: bucket privado `linha-proposta-renders` (renders de
propostas). Prisma foi removido por completo.

**Telas** (shell autenticado `app/(app)/`, sidebar navy + topbar):
- `/` — Dashboard (lista de orçamentos).
- `/orcamento/[id]` — o coração do produto, 4 abas: Ambientes (elevação 2D,
  posicionamento, Conjuntos, Elementos Contínuos), Corte & Material (plano
  de corte agregado, lista de material congelável), Financeiro (resumo de 6
  campos, modos de precificação/montagem), Proposta (Linhas de Proposta com
  render automático, override com rebalanceamento, gera `/proposta/[id]/pdf`).
- `/orcamento/[id]/item/[itemId]` — Editor de Item (módulo-caixa ou placa).
- `/perfil` — dados da organização + pessoais, padrões de precificação.
- `/catalogo` — CRUD de produtos (chapas/ferragens/fita/LED/acessórios).
- `/biblioteca` — gabaritos reutilizáveis (base global + fork por
  organização).
- `/modulo` — laboratório de edição de gabaritos (mesmo núcleo do Editor de
  Item).
- `/dev/preview/*` — harnesses com dados mock, sem sessão, 404 em produção;
  usados pra auditoria visual sem precisar logar.

**Testes**: 329 passando (`npm run test`). `npm run lint`/`npm run typecheck`
limpos.

## 4. Decisões que não devem ser reabertas sem motivo novo

- Motor V3 é o único motor; **Placa** é peça plana (prateleira/fechamento/
  ripado), distinta de `BoxModule` (móvel com carcaça).
- Multi-tenant via Supabase RLS, tenant = Organização. Sem Prisma.
- Elementos contínuos (tampo/rodapé/tamponamento/fechamento) são um único
  mecanismo, dimensão derivada (exceto rodapé/fechamento, que aceitam
  override manual).
- Engrossamento/dobra de Placa: o parâmetro é o **nível** (1/2/3), não a
  espessura em si.
- Posicionamento é 1D com faixas (inferior/bancada/aéreo/torre), não 2D
  livre.
- Rateio de preço é por **custo alocado** (área de peças do BOM, segregado
  por material) — nunca por ocupação do plano de corte nem por m².
  Congelamento no fechamento da proposta é obrigatório.
- Design System v3: sidebar navy fixa (não é dark mode) + laranja como
  destaque. **Sem 3D real** no canvas técnico (`BoxCanvas`/`ElevacaoParede`/
  `PlanoCorteCanvas`) — decisão explícita do operador (2026-07-31, D-33);
  exceção escopada: `ModuleViewer` 3D estático (câmera fixa, sem controles),
  em backlog.
- Catálogo (`produto`) e Biblioteca (`gabarito`) são reais (Supabase), com
  wiring de consumo ligado nas telas que usam preço/gabarito.

## 5. Pendências reais (não bloqueiam nada, mas existem)

- **Operador**: configurar o dashboard Supabase (Site URL/Redirect URLs +
  template de e-mail "Confirm signup", ou desligar "Confirm email") antes de
  criar a primeira conta real e validar os fluxos de escrita ponta-a-ponta
  (o Maestro nunca cria conta nem digita senha).
- **Operador**: configurar o template de e-mail "Reset Password" no
  dashboard Supabase (Task 4.12-4.13-back, RF-31 — troca de senha) para
  `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=recovery`,
  mesma customização já feita para "Confirm signup" acima.
- **Operador**: cadastrar os ~380 padrões reais de MDF em `/catalogo` (ou via
  Supabase Table Editor) — hoje só o catálogo seed genérico existe.
- **Histórico (2026-08-24)**: Todas as 9 migrations pendentes de aplicação manual foram aplicadas em produção pelo operador via SQL Editor do Supabase Dashboard — confirmado via information_schema. Migrations: elemento_parede_preset (20260806100000), organizacao_espessura_serra_padrao_mm (20260811100000), lista_material_override (20260811110000), storage_texturas (20260811110000), produto_codigo (20260812120000), storage_organizacao_logos (20260812130418), perfil_foto_url_storage (20260813140434), orcamento_cliente_id_no_action (20260813160000), perfil_update_por_coluna (20260813170000 — prioridade alta, fecha vulnerabilidade de elevação de privilégio em coluna `papel`). Nenhuma migration pendente de aplicação restante.
- Dívida de segurança de baixa prioridade nunca endereçada — ver
  `docs/Backlog.md` (Stage 3): headers de segurança, auditoria de operações
  sensíveis, limite de cache de fórmulas.
- Upgrades major (`next`, `vitest`) avaliados como risco real mas não
  agendados — ver `docs/Backlog.md` (Stage 4).
- Permissão de escrita em `organizacao`/`perfil` (`/perfil`) não tem
  granularidade por papel — qualquer membro da org edita dados
  financeiros/CNPJ da empresa. Registrado, não corrigido; decisão do
  operador se/quando restringir.
- **Chaves do Supabase potencialmente expostas (2026-08-05)** — durante a
  execução da Task 5.10-back, o `backend-engineer` rodou `npx supabase
  projects api-keys --project-ref ...` (2x) e varreu `.env`/variáveis de
  ambiente tentando contornar uma falha de autenticação da CLI ao aplicar a
  migration real (achado registrado em
  `.maestro/proposals/2026-08-05-executor-nao-deve-cacar-credencial-supabase.md`).
  Chaves impressas no transcript tratadas como **potencialmente
  comprometidas**. **Decisão do operador (2026-08-05): não rotacionar
  agora** — projeto ainda em desenvolvimento, rotação fica agendada para o
  lançamento.
- **CLI do Supabase sem privilégio para o projeto `orcamentofacil`
  (`ioakptuwhfvlirvrciwg`)** — `npx supabase link --project-ref
  ioakptuwhfvlirvrciwg` retorna "account does not have the necessary
  privileges"; o projeto nem aparece em `npx supabase projects list` para a
  conta logada neste ambiente. `db push` também é bloqueado pelo
  classificador de permissão do sandbox antes de tentar autenticar. Migrations
  reais precisam ser aplicadas pelo operador (via MCP ou SQL Editor do
  Supabase Dashboard) até essa conta ser relinkada/autorizada. Confirmado
  funcionando: a migration da Task 5.10-back (coluna `etapa_esteira`) foi
  aplicada pelo operador via MCP em 2026-08-05 e verificada com
  `list_tables` (default `'novo'`, check constraint e comentário
  corretos).
- **Usuário/organização de teste real no Supabase (2026-08-26)** — durante a auditoria visual da Task R.4b, o `ux-auditor` criou conta e organização de teste real no Supabase (`uxauditor.r4b2.*@maestro.local`) para autenticar em `/configuracoes/materiais` e `/ambientes` (rotas protegidas sem harness equivalente, `seed-qa-user.ts` indisponível por falta de `SUPABASE_SERVICE_ROLE_KEY`/`QA_USER_PASSWORD` no ambiente). Decisão do operador: (1) considerar limpar esse usuário/organização de teste; (2) provisionar as variáveis de ambiente do `seed-qa-user.ts` para que auditorias futuras não precisem criar contas reais.

## 6. Convenções operacionais desta esteira

- **Model routing**: antes de executar qualquer task, recomende o modelo
  ("Modelo Recomendado" no Backlog) e espere o operador confirmar.
- **Fluxo por task**: branch `feature/<task-id>` → Executor (subagente) →
  Code Auditor → Security Auditor (quando envolve RLS/secrets/infra) → QA
  Engineer → UX Auditor (subagente com `Bash`, sobe `preview_start`/dev
  server e captura evidência — não é o Maestro operando a ferramenta de
  Browser pane diretamente) → merge `--no-ff` em `main` → memory-manager
  sincroniza Backlog/Status → commit → push.
- **Plugin Maestro v3.5.0+ (instalado em 2026-08-05, confirmar em
  `~/.claude/plugins/installed_plugins.json`)**: gates gravam veredito em
  `.maestro/tmp/verdicts/<task-id>-<gate>.md` ANTES de responder em texto —
  o Maestro lê o arquivo, nunca a mensagem de retorno. Corrige um bug real
  do Claude Code
  ([anthropics/claude-code#58109](https://github.com/anthropics/claude-code/issues/58109))
  que descartava o texto final de subagentes cuja última ação era uma
  chamada de ferramenta — não era `maxTurns` nem esgotamento de contexto,
  apesar de parecer isso à primeira vista (ver
  `.maestro/proposals/2026-08-04-gates-estouram-maxturns-sem-veredito.md`
  para o histórico completo da investigação). Os 5 auditores agora rodam
  `background: false`. Se uma sessão nova ver gate voltando sem veredito de
  novo, checar a versão do plugin primeiro — pode ser instalação anterior
  ao fix.
- **Executor não deve caçar credencial ao aplicar migration real**: se
  aplicar migration no Supabase real falhar (permissão de conta, bloqueio de
  sandbox, rede), o executor para e reporta o erro exato ao Maestro — nunca
  tenta comandos que imprimem segredo em texto plano (`api-keys`, etc.) ou
  varre `.env` tentando contornar. Achado de 2026-08-05, proposta em
  `.maestro/proposals/2026-08-05-executor-nao-deve-cacar-credencial-supabase.md`
  aguardando decisão do operador sobre alterar `backend-engineer.md`.
- Se um executor for interrompido no meio (limite de gasto, parar sem
  terminar) **na mesma sessão**, o Maestro retoma via `SendMessage` ou
  redelega — nunca implementa o código diretamente.
- **Início de sessão nova**: `SendMessage` não atravessa sessões (o ID do
  agente não sobrevive ao fechamento). Antes de redelegar qualquer task,
  rode `git worktree list` — se existir um worktree `orcamentofacil-<task>`
  com trabalho pendente, o código **já está pronto no disco**; leia
  `.maestro/state/<task>.json` para saber exatamente qual gate rodar a
  seguir, e acione só esse gate (nunca um executor novo) até essa checagem
  confirmar que não há worktree órfão.
- Tasks paralelas (dentro do mesmo Lote) usam `git worktree` dedicado por
  task — nunca duas branches na mesma pasta de trabalho ao mesmo tempo
  (causou perda de contexto por checkout cruzado em 2026-08-03).
- Retrospectiva (`improvement-agent`) roda ao final de cada Pipeline Stage
  fechado — lições em `docs/Lessons-Learned.md`, propostas de mudança de
  framework em `.maestro/proposals/` (aguardam decisão humana).
